# .env.example — Copy to .env and fill in values
# cp .env.example .env && nano .env

# REQUIRED: API authentication key (generate with: openssl rand -hex 32)
VGGT_API_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# Model weights SHA-256 hash (get from: docker run vggt-secure audit)
# Leave empty to skip verification (NOT recommended for production)
VGGT_MODEL_HASH=

# Path to directory containing model.pt on the HOST machine
VGGT_MODEL_DIR=./model

# Path to shared data directory on the HOST machine
VGGT_DATA_DIR=./data

# External port to expose the API on
VGGT_EXTERNAL_PORT=8000

# Processing limits
VGGT_MAX_IMAGES=50
VGGT_MAX_RESOLUTION=2048
VGGT_MAX_UPLOAD_MB=200
VGGT_RATE_LIMIT_RPM=30

# Logging
VGGT_LOG_LEVEL=INFO
